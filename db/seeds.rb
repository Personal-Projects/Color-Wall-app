# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the rails db:seed command (or created alongside the database with db:setup).
#
# Examples:
#
#   movies = Movie.create([{ name: 'Star Wars' }, { name: 'Lord of the Rings' }])
#   Character.create(name: 'Luke', movie: movies.first)

# 100.times do
#   Color.create(
#     name: Faker::Color.color_name,
#     hex: Faker::Color.hex_color,
#     user_id: rand(1...2)
#   )
# end

# 100.times do
#   Card.create(
#     name: Faker::Job.field,
#     color_one: Faker::Color.color_name,
#     color_two: Faker::Color.color_name,
#     color_three: Faker::Color.color_name,
#     user_id: rand(1...2)
#   )
# end

# 100.times do
#   Project.create(
#     name: Faker::Job.field,
#     start_date: Faker::Date.backward(14),
#     finish_date: Faker::Date.forward(23),
#     concept: "A brilliant concept here.",
#     user_id: rand(1...2)
#   )
# end

# projects = Project.all
# projects.each do |project|
#   project.update(user_id: rand(1..2))
# end

# colors = Color.all
# colors.each do |color|
#   color.update(user_id: rand(1..2))
# end

# cards = Card.all
# cards.each do |card|
#   card.update(user_id: rand(1..2))
# end

